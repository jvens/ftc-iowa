

<%= form_for :team, url: team_path(@team), method: :patch, multipart: true, html: { :'data-ajax' => false } do |f| %>
	<% if @team.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@team.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @team.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
	<% end %>
	
	<%= f.label :description %>
	<%= f.text_area :description %>
	
	<%= f.label :logo %>
	<%= f.file_field :logo %>
	
	<%= f.label :picture %>
	<%= f.file_field :picture %>

	<%= f.submit %>
<% end %>